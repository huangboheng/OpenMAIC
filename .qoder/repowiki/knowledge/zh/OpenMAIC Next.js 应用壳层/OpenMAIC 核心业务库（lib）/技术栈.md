TypeScript + Next.js（通过 process.env.NEXT_PUBLIC_BASE_PATH 识别部署环境），Node.js 运行时（process.env 环境变量控制 LOG_LEVEL、LOG_FORMAT），浏览器 API 拦截（window.fetch patching），LangGraph StateGraph 用于多 Agent 编排，partial-json + jsonrepair 用于流式结构化输出解析。