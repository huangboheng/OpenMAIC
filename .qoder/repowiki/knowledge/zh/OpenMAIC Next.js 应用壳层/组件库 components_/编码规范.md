- 所有组件文件使用 'use client' 指令标记客户端组件
- 通过 @/lib/store 中的 zustand store 进行跨组件状态共享，避免 props drilling
- 复杂交互拆分为 hooks（如 useEditModeLock、use-quiz-surface.ts）与纯组件分离
- 子目录按功能域组织，每个目录暴露独立的 index.ts 或主组件作为入口
- UI 基础组件集中在 ui/ 目录，业务组件通过组合这些原子组件构建