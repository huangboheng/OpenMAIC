React Server Components + Next.js App Router；动画使用 `motion/react`（Framer Motion）与 `AnimatePresence`；UI 基础组件来自 `@/components/ui/*`（Radix 封装）；图标使用 `lucide-react`；通知使用 `sonner`；状态管理基于 Zustand store（`useStageStore`、`useSettingsStore`、`useUserProfileStore`）。